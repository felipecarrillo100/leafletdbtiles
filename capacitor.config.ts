import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.felipecarrillo100.leafletdbtiles',
  appName: 'leafletdbtiles',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
